syntax = "proto3";

option go_package = "github.com/lshtar13/blockchain/node/global";

package global;

import "protos/types.proto";

service TxSrv {
  rpc ReqTx(TxReq) returns (stream protos.Tx);
  rpc SendTx(protos.Tx) returns (protos.Ack);
}

message TxReq { repeated bytes hash = 1; }
